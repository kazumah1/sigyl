---
title: "Quick Start"
description: "Get started with Sigyl CLI and SDK in minutes."
---

# Quick Start

<Callout type="tip">
  You can use either the CLI or SDK to get started with Sigyl. Choose the method that fits your workflow!
</Callout>

<Card
  title="CLI"
  description="Install and use any public MCP package instantly."
  icon="terminal"
  href="/cli-reference"
/>
<Card
  title="SDK"
  description="Integrate Sigyl programmatically in your JavaScript/TypeScript projects."
  icon="code"
  href="/sdk-reference"
/>

<CodeGroup>
  <CodeGroup.Tab label="CLI">
    {`npx @sigyl-dev/cli install <package-name>`}
  </CodeGroup.Tab>
  <CodeGroup.Tab label="SDK (TypeScript)">
    {`npm install @sigyl-dev/sdk\n\nimport { connect } from '@sigyl-dev/sdk';\nconst summarize = await connect('text-summarizer', 'summarize');\nconst result = await summarize({ text: 'Long text...', maxLength: 100 });`}
  </CodeGroup.Tab>
</CodeGroup>

```